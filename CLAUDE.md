# CLAUDE.md

## このリポジトリ

`x-auto-poster` — RSSフィードから直近のニュースを集め、「事実」と「意見」に分けて
`docs/index.html` を毎朝再生成し、GitHub Pages で配信する静的サイト。
仕組み・セットアップ・フィードの追加方法は `README.md` を参照。

- 生成スクリプト: `scripts/generate_site.py`
- フィード定義: `config/feeds.json`
- 自動更新: `.github/workflows/daily-update.yml`（毎日 06:00 JST）
- 自動コミットは `docs/index.html` のみを対象にする。生成物以外をこのワークフローに含めない。

---

# 全作品共通ルール（KDP制作）

出典は制作台帳（Artifact: `KDP制作台帳` / ec375402-8b6a-4550-a16d-b34ae6195ca4）。
ここには **全作品に共通して適用する取り決め** だけを置く。作品ごとの設定・年表・進捗は
各作品のノート側に置き、ここには書かない。

> 置き場所の注意: これらは本来メモリポジトリ `boundary-japan-memos` に属する内容。
> このリポジトリには可搬な形で置いてあるだけなので、メモリポジトリ側へ移す／複製する際は
> `CLAUDE.md` のこのセクションと `.claude/skills/kdp-*` をまとめて移動する。

## 体制・前提

- **日本語オリジナルを先に完成させ、そこから各言語へ文化的ローカライズする。**
  展開先は英語・ドイツ語・フランス語・スペイン語・イタリア語・オランダ語・ブラジルポルトガル語。
  翻訳ではなく文化的ローカライズとして扱う。対応言語は随時追加する。
- **作業時間は1日およそ2時間を上限として設計する。**
  AI併用を前提に、この枠に収まる工程分割で進める。
- **繰り返し使う制作ルールはメモリファイル（CLAUDE.md／SKILL.md）に集約する。**
  作業から得た知見は CLAUDE.md に書かず、Obsidian のナレッジベースへ置く。
- 周辺の仕組み: Obsidian + Claude Code による知識管理、メモリリポジトリ
  `boundary-japan-memos`、X自動投稿の `x-auto-poster`。

## ナレッジは自動で回す

作品個別ではなく、全作品共通の汎用ルールとして常時適用する。

- 作業完了時に Obsidian へ記録する。
- 投稿・入稿後、完成版と元原稿の差分を分析する。
- **次の作業に着手する前にナレッジを参照する。**

## 工程ごとの詳細ルール

工程の具体手順は Skill 側にある。該当する作業に入るときに読むこと。

| 工程 | Skill |
| --- | --- |
| 01 執筆・引きの設計 ／ 02 推敲・癖チェック | `.claude/skills/kdp-manuscript/SKILL.md` |
| 03 docx制作 | `.claude/skills/kdp-docx/SKILL.md` |
| 04 KDPメタデータ | `.claude/skills/kdp-metadata/SKILL.md` |

ルールを追加・変更したときは、制作台帳の Artifact も合わせて更新する。
