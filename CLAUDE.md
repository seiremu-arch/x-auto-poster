# CLAUDE.md

## このリポジトリ

2つのものが同居している。

1. **分岐点ニュース** — RSSを「事実」「意見」に分けて毎朝自動更新する静的サイト(`scripts/` `config/` `docs/` `.github/`)
2. **Obsidian vault** — 案件とナレッジを貯める `vault/`

## vault/ の扱い(重要)

`vault/` はObsidianのvaultそのもの。作業で得た知見は、ここに**新しいフォルダ/ファイルとして足していく**。

- 案件ごとに `vault/案件/<案件名>/` を作り、同名のハブノートを置く
- 再利用できる知識は `vault/ナレッジ/<分野>/<トピック>.md` に切り出す
- 既存ノートに書き足して肥大させるのではなく、**新しいノートに分けて `[[ウィキリンク]]` で繋ぐ**
- ノートを作ったら、上位ノート(案件ハブ / `vault/ホーム.md`)からのリンクと、関連ノートへのリンクを必ず張る

詳しい取り決めは `vault/運用ルール.md` にある。**vault内のノートを作る前に必ず読むこと。**

### 作業のたびにやること

このリポジトリで実装・調査・運用対応をしたら、その内容を該当する案件ノートに反映し、案件をまたいで使える知見はナレッジノートに切り出す。コードだけ変えてvaultを放置しない。

### 便利スクリプト

```bash
# 雛形を作り、ホーム.md へのリンクまで張る
python scripts/new_note.py 案件 "案件名"
python scripts/new_note.py ナレッジ "トピック名" --分野 GitHub

# リンク切れ・孤立ノートの検査
python scripts/check_vault_links.py
```

ノートを追加・改名したら `check_vault_links.py` を通してからコミットする。

## 生成物

`docs/index.html` は `scripts/generate_site.py` の生成物。**手で編集しない**(次回の自動実行で上書きされる)。
